# Project instructions

Build a small Python CLI app.

Rules:
- Use Python 3.11+
- Use Pydantic
- Keep code simple
- No web UI yet
- No database yet
- No agent framework yet
- Goal is only to load stackpilot.yml files from sibling folders and print inventory
- Add pytest tests for core functions